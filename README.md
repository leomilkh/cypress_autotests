# these are some Cypress+JS autotests

to find tests —> [cypress] —> [e2e]

to run via browser in real time: <i>npx cypress run --headed --spec cypress/e2e/pokemons_buy_avatar.cy.js --browser [your browser]</i>
